rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write access to anyone, including unauthenticated users.
      // This is the simplest possible configuration and removes all permission checks.
      allow read, write: if true;
    }
  }
}
